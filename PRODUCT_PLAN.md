# AgentSystem — 产品规划（PRD，平台级）

> 视角：产品经理
> 范围：**整个 AgentSystem 平台**（AI 原生应用操作系统），小说工作室（novel_studio）作为旗舰应用案例贯穿。
> 关联：技术优化清单见 `OPTIMIZATION_TASKLIST.md`（已扩展平台级）。

---

## 一、产品定位

**一句话**：AgentSystem 是「**AI 原生应用操作系统**」——像操作系统管理进程一样，构建、运行、治理 AI 原生应用（App）。

**它不是**：一个聊天机器人、一个脚本框架、一个写小说的工具。（这些都只是它承载的"应用"。）

**它是什么**：
- **内核** = 主控（Master Control）：统一权限审批、能力声明、系统管理、审计。
- **应用层** = App（如小说工作室）：由"技能 + 资产 + 模型 + 上下文"声明式组装的可运行应用。
- **能力层** = Skill / Asset：可复用的能力单元，像"系统调用"。
- **运行时** = 上下文（Context）、模型路由（Model）、任务调度（Task）。
- **治理层** = 权限、成本配额、审计日志、回归测试、可观测性。

**类比**：Linux = 内核 + 系统调用表 + 进程；AgentSystem = 主控 + 能力清单 + AI 应用。

---

## 二、目标用户与价值

| 角色 | 诉求 | 价值 |
|------|------|------|
| **终端用户** | 用上"能对话、能干活"的 AI 应用 | 应用商店般的体验：选 App → 对话使用 |
| **应用开发者** | 不写死代码、声明式构建 AI 应用 | 用 Skill/Asset 组装，能力可复用 |
| **平台运维者** | 可控、可审计、可观测、可升级 | 统一治理（权限/成本/审计/回归）|

**平台级核心价值主张**（按优先级）：
1. **可复用**：能力以 Skill/Asset 沉淀，新 App 是"组装"而非"重写"。
2. **可治理**：所有操作经主控授权，权限/成本/审计/回归可追溯——这是"操作系统"区别于"脚本"的根本。
3. **可运行**：会话上下文 + 模型路由 + 任务调度，支撑多轮、长时、并发的真实运行。
4. **可演进**：App 全生命周期（创建→运行→升级→回滚），自我迭代。

---

## 三、平台核心旅程

**终端用户主链路**：
`登录 → 选 App(小说工作室/对话) → 使用(生成/阅读/聊天) → 会话切换 → 结果导出`

**开发者主链路**：
`创建 App → 组装 Skill/Asset → 配置模型/上下文 → 治理策略 → 发布/迭代`

**运维主链路**：
`监控 → 审计 → 成本 → 回归测试 → 升级/回滚`

---

## 四、平台功能地图（按价值分层）

### P0 — 平台基石（必须稳定，当前已有骨架）
- **主控（内核）**：能力声明、权限审批、统一执行、审计日志
- **App 生命周期**：创建 / 配置 / 运行 / 停止 / 删除 / 升级 / 回滚
- **Skill / Asset 系统**：注册、安装、清单、能力复用
- **对话 + 会话**：多轮上下文、会话隔离与切换
- **模型路由**：多 provider、按能力路由、上下文注入
- **认证权限**：登录、角色、操作授权

### P1 — 好用性（产品价值提升点）
- **应用市场/工作台**：一站浏览、一键启用内置 App（小说工作室、对话助手…）
- **治理可视化**：成本/配额、审计流水、回归趋势 的操作台（已有部分 API）
- **上下文可感知**：用户能看到"系统记住了什么"、能管理会话记忆
- **错误可解释**：任务失败定位到能力+根因，提供重试/回滚
- **开发者工具**：Skill 脚手架、App 组装向导、调试日志

### P2 — 进阶（加分）
- **App 模板/克隆**：一键复制应用配置作为新应用起点
- **多租户**：组织/用户级隔离与配额
- **能力市场**：第三方 Skill 分享与订阅
- **自动化治理**：策略驱动的回归/告警/成本封顶

---

## 五、优先级决策（产品经理判断）

1. **先稳内核**：主控授权链、App 生命周期、Skill/Asset 复用、会话上下文——平台地基，任一不稳全盘不稳。
2. **再顺主链路**：终端用户"选→用→管"要顺滑，旗舰 App（小说工作室）作为体验样板。
3. **后加治理可视化**：成本/审计/回归看板，让平台"可信可用"。
4. **最后加花活**：模板、多租户、能力市场。

**当前最大体验断点**：
- 平台层面：**入口分散**——CLI、多个 Web 页（/chat、/studio、/debug-log）、API 并存，没有统一"工作台"。
- 能力层面：**能力可发现性弱**——用户/开发者不清楚平台有哪些 Skill/Asset/App，能力清单没有友好的 UI。
- 可靠性层面：**治理与可观测数据存在但未产品化**——回归、审计、成本 API 已实现，但没有面向用户的操作台聚合。

---

## 六、成功指标（SMART）

| 维度 | 指标 | 衡量 |
|------|------|------|
| 可用性 | 主链路 E2E（登录→用 App→导出）通过 | Playwright |
| 稳定性 | 连续运行无崩溃、任务失败率 0 | 任务 error 率 |
| 可治理 | 每个修改操作都有审计记录 + 授权 | 审计完整性 |
| 可复用 | 新 App 复用了 ≥1 个现有 Skill/Asset | 组装清单 |
| 可观测 | 任务/成本/审计/回归可查询 | 看板覆盖 |

---

## 七、产品 ←→ 技术 映射（衔接 OPTIMIZATION_TASKLIST.md）

| 产品诉求 | 技术支撑 |
|----------|----------|
| 平台地基稳定 | T2（可靠性：并发锁、任务持久化、LLM 重试、数据自愈）——通用到整个平台，不只小说 app |
| 统一工作台 | T4.1（拆分散入口为统一 UI 壳）+ T4.3（应用市场/治理看板） |
| 能力可发现 | T4.2（重构 worker op_map → 能力清单驱动）+ 能力清单 UI |
| 旗舰 App 体验 | 小说 app 全部 T2-T6 项 |
| 可观测/治理产品化 | T3（结构化日志、指标、错误归因）+ 治理看板 |

---

## 八、一句话总结（PM 视角）

> AgentSystem 是「AI 原生应用的操作系统」。产品工作的主线，是让"**能力可复用、操作可治理、体验可顺滑、状态可观测**"——小说工作室是验证这套 OS 的第一个旗舰应用。
> 优化的终极目标，是让下一个 App 的诞生像"装一个软件"一样自然，而不是"写一套系统"。
